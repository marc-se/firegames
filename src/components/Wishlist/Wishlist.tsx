import React from "react";

import Head from "../Head/Head";

interface Props {}

const Wishlist = (props: Props) => {
	return (
		<React.Fragment>
			<Head />
			<div>Wishlist</div>
		</React.Fragment>
	);
};

export default Wishlist;
